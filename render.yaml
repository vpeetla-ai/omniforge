services:
  - type: web
    name: omniforge-api
    runtime: python
    plan: free
    buildCommand: pip install -e ".[api]"
    startCommand: gunicorn api.main:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    healthCheckPath: /health
    autoDeploy: true
    branch: main
    envVars:
      - key: APP_ENV
        value: production
      - key: OMNIFORGE_MODE
        value: mock
      - key: CORS_ORIGINS
        value: https://omniforge.vercel.app,https://venkat-ai.com
